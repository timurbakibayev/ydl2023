def summa(a: int, b: int) -> int:
    """
    Sums two number
    :param a:
    :param b:
    :return:
    """
    assert isinstance(a, int)
    assert isinstance(b, int)
    assert a > 0
    return a + b
